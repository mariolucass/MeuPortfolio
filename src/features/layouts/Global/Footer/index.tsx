import * as styled from "./styles";
import { motion } from "framer-motion";

export const Footer = () => (
  <styled.Footer>
    <motion.div></motion.div>
    <motion.div></motion.div>
  </styled.Footer>
);

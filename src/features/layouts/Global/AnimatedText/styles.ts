import { motion } from "framer-motion";
import styled from "styled-components";

export const AnimatedTextTitle = styled(motion.h2)`
  font-size: 3rem;
  font-weight: 600;
`;

export const AnimatedTextCharacter = styled(motion.h2)`
  display: inline-block;
  margin-right: -0.05em;
`;

import { motion } from "framer-motion";
import * as styled from "./styles";

export const Footer = () => {
  return (
    <styled.Footer>
      <div></div>
      <div></div>
    </styled.Footer>
  );
};
